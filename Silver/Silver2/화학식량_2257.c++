#include <iostream>
#include <map>

using namespace std;

map<char,int> m;
string s;

int solve(string s){
	while(s)
}

int main(){
	m.insert({'H',1});
	m.insert({'C',12});
	m.insert({'O',16});
	cin >> s;
	solve(s);
}